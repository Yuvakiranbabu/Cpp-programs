#include<iostream>
using namespace std;

int main()
{
    int length;
    int width;
    cout<<"Enter the length and width of the box seperated by a space: ";
    cin>>length>>width;
    int area_of_box=length*width;
    cout<<"Area of the given box is "<<area_of_box;
}
